Signup
    ---
    tags
    parameters:
      - name: body
        in: body
        required: true
        type: string
        description: This is an endpoint for creating a user

    Responses:
      201:
        description: account created
